package com.ds.linkedlist;

import java.util.function.BiFunction;

import com.ds.graph.AdjacentMatrix;
import com.ds.tree.FenwickTree;
import com.ds.tree.SegmentTree;
import com.ds.tree.SegmentTree2;
import com.ds.tries.StandardTries;

public class Main {
	public static void main(String[] args) {
	  System.out.println(6.0/4.0+" "+Math.ceil((double) 3 / (double) 4));
	}
}
